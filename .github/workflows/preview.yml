name: Build Docker Images (Preview)

on:
  push:
    branches:
      - preview

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}
     
      - name: Build and Push Multi-Arch Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/dashwise:${{ secrets.VERSION }}-pre
            ${{ secrets.DOCKER_USERNAME }}/dashwise:pre-release
            ${{ secrets.DOCKER_USERNAME }}/dashwise:preview
      
      - name: Build and Push Multi-Arch Pocketbase Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./pocketbase/
          file: ./pocketbase/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/dashwise-pb:${{ secrets.VERSION }}-pre
            ${{ secrets.DOCKER_USERNAME }}/dashwise-pb:pre-release
            ${{ secrets.DOCKER_USERNAME }}/dashwise-pb:preview


      - name: Build and Push Multi-Arch Job runner Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./jobs/
          file: ./jobs/Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/dashwise-jobs:${{ secrets.VERSION }}-pre
            ${{ secrets.DOCKER_USERNAME }}/dashwise-jobs:pre-release
            ${{ secrets.DOCKER_USERNAME }}/dashwise-jobs:preview
